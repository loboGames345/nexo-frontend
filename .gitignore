# --- Dependencias (Pesan mucho, nunca se suben) ---
node_modules/
*/node_modules/
backend/node_modules/
frontend/node_modules/

# --- Variables de entorno (SECRETOS) ---
.env
*/.env
backend/.env

# --- Archivos de sistema y logs ---
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # (Opcional: algunos prefieren subirlo, otros no. Si usas yarn, borra esta linea)

# --- Build de producción de React (Se genera solo) ---
build/
dist/
frontend/build/